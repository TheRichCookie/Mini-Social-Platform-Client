export enum UkSearchBarStatus {
  NORMAL = 'NORMAL',
  ACTIVE = 'ACTIVE',
}
export type SearchBarStatus = `${UkSearchBarStatus}`;

export enum UkSearchBarColor {
  PRIMARY = 'PRIMARY',
  SECONDARY = 'SECONDARY',
}

export type SearchBarColor = `${UkSearchBarColor}`;

export enum UkSearchBarBorderColor {
  PRIMARY = 'PRIMARY',
  SECONDARY = 'SECONDARY',
}

export type SearchBarBorderColor = `${UkSearchBarBorderColor}`;
